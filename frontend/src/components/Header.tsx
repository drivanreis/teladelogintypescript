// src/components/Header.tsx

import React from 'react';
import './Header.css'; // Importando estilos do cabeçalho

const Header: React.FC = () => {
  return (
    <header>
      <h1>Meu Aplicativo</h1>
    </header>
  );
};

export default Header;
